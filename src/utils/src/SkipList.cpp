#include "SkipList.h"


namespace SG {
namespace Core {
}
} // namespace SG


/*
template <typename TK, typename TV>
class SkipList {
public:
    SkipList();
    ~SkipList();
    TV&  search(const TV& value);
    void insert(const TV& value);
    void dump();

    static std::vector<TV> combine(const std::vector<SkipList<TK, TV>*>&);
};
*/